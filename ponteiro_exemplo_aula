#include <stdio.h>
#include <stdlib.h>

int main()
{
   /*int *p1,*p2,*soma;
   
   p1= malloc(sizeof(int));
    scanf ("%d",p1);
   p2= malloc(sizeof(int));
    scanf ("%d",p2);
   soma= malloc(sizeof(int));
   printf("digite p1: \n");
   scanf("%d",p1);
   printf ("digite p2: \n");
   scanf ("%d",p2); 

   *soma= *p1+*p2;
   printf ("soma:%d", *soma);*/
   
   /*int *vet,i,soma=0;
   vet=malloc(3*sizeof(int));
   for (i=0;i<3;i++)
   {
     printf ("digite valor: ");
     scanf ("%d",&vet[i]);
   }
   
   for (i=0;i<3;i++)
   {
     soma= soma+vet[i];
   }

  printf ("media: %d\n",soma/3);*/
  
  //transformando soma em ponteiro
  
  int *vet,*i,*soma;
   soma=malloc(sizeof(int));
   *soma=0;
   vet=malloc(3*sizeof(int));
   i=malloc(3*sizeof(int));
   for (*i=0;*i<3;(*i)++)
   {
     printf ("digite valor: ");
     scanf ("%d",&vet[*i]);
   }
   
  for (*i=0;*i<3;(*i)++)
   {
     *soma= *soma+vet[*i];
   }

  printf ("media: %d\n",*soma/3);
}
